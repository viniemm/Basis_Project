import pandas as pd
import yfinance as yf
import numpy as np
import matplotlib as plt

class Fetcher:
    # Gets the information about each of the stock sent to it.
    # Like 5 or 10 year history, description, name, etc.

    def screen(self):
        # Screens and finds the stocks with the given criteria in a list format.
        pass
